# raw/pointers/

Stub directory for Source_Library path pointers and ingest notes.

Populated in **S2** (Source import). Each pointer file should cite local paths under `C:\Personal\40K` or living web URLs — never embed GW binaries.
