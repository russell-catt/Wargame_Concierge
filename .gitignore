# Python / tooling
.venv/
__pycache__/
*.pyc
*.egg-info/

# Secrets — never commit
.env
.env.*
secrets/
credentials.json
*.pem
*.key

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Scratchpad / ephemeral (Rising Tide pattern)
scratchpad/
Scratchpad/
**/scratchpad/**

# GW official binaries — NEVER commit (path pointers in markdown OK)
*.pdf
*.PDF
*.webp
*.WEBP
*.png
*.jpg
*.jpeg
*.gif
*.bmp
*.tiff
*.tif

# Warcode free beta rulebook only — RedMakers public beta (not GW)
!raw/the_warcode/
!raw/the_warcode/**
!raw/the_warcode/*.pdf
!raw/the_warcode/*.PDF
!raw/the_warcode/**/*.pdf
!raw/the_warcode/**/*.PDF

# Allow markdown and Obsidian vault config
!**/*.md
!.obsidian/**
!.obsidian/**/*

# Build / output (future)
output/**
!output/README.md
