# S3 — Brief (Kill Team 2024 shipping impact)

- **Track:** `dataslate_0826`
- **Slice:** S3
- **Status:** Ready
- **Depends:** QA-S1 PASS (may run parallel with S2)
- **Recommended models:** Implementer `claude-sonnet-5-thinking-high` · QA `gpt-5.6-sol-high`

## Inherited documentation

- S0 impact matrix (KT rows)
- `games/kill_team_2024/**` — rules, teams (Canoptek Circle, Plague Marines, Angels of Death priority), print, Event_Ready
- KT24 quote hierarchy (Full-Scan baseline; dated `eng_*` supersede)

## Requirements

1. Walk S0 KT impact matrix; update teaching docs / team notes / print aids as needed.
2. If dataslate patches a quoted surface under Sec 10 KT exception: update quote + cite (filename + page) **or** demote to paraphrase if unclear — never leave a superseded verbatim block unmarked.
3. Stamp KT **Rules currency** line with locked KT dataslate date on touched GW notices/footers.
4. Write `S3_implementer.md` with file list + no-op waivers.
5. Do not expand into parked `kt24_doc_followups` unless dataslate forces a touch (note in implementer if so).

## Exit criteria (QA verifies)

- [ ] Currency stamps use locked KT date
- [ ] Quote hierarchy respected; superseded quotes flagged or updated
- [ ] Regression bar for cheat sheets / Event_Ready
- [ ] Legibility spot-check ≥3 changed pages
- [ ] No binaries; subagent did not git

## Constraints

Read PDFs in place only. Personal use / never for sale language stays.
